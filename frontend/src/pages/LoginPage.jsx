/**
 * LoginPage.jsx — Student Login Screen
 * ======================================
 * The entry point for existing students. This is also the default landing page
 * (App.jsx redirects "/" here).
 *
 * TODO (Phase 3 — Frontend):
 *  - Email + password input fields
 *  - "Don't have an account? Register" link to /register
 *  - On submit: POST /api/auth/login with credentials
 *  - On success: store JWT token (context/localStorage), redirect to /verify
 *  - On failure: show error message
 */

function LoginPage() {
  // Placeholder UI — real form will be built in Phase 3
  return <div><h1>Login Page</h1></div>
}

export default LoginPage
