/**
 * RegistrationPage.jsx — New Student Registration
 * =================================================
 * First-time students create their account here.
 *
 * TODO (Phase 3 — Frontend):
 *  - Name, email, password input fields
 *  - Webcam component to capture a face photo for registration
 *  - On submit: POST /api/auth/register with form data + face image
 *  - On success: redirect to /login with a "Registration successful" message
 *  - On failure: show validation errors
 */

function RegistrationPage() {
  // Placeholder UI — real form + webcam capture will be built in Phase 3
  return <div><h1>Registration Page</h1></div>
}

export default RegistrationPage
